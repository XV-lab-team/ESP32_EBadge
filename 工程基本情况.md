# ESP32_EBadge 工程基本情况

日期：2026-08-25  
仓库：<https://github.com/XV-lab/ESP32_EBadge>  
本文根据当前仓库源码、`sdkconfig` 和已有排查记录整理，便于后续接手。

---

## 产品是什么

这是一块 **电子吧唧（E-Badge）** 自制板，MCU 用乐鑫 ESP32-S3。  
硬件上已经有屏、SD 卡、LED、扩展 IO 等。当前固件是 **ST77916 一线写屏 + LVGL 9 已出字**，**SDMMC 4-bit + FAT（`/sdcard`）**，以及 **配置模式 USB MSC（U 盘）**。点亮前提仍是 HD18004C18 + INVON + 一线 16-bit。没有做无线 / 吧唧菜单。CDC 未做。

CMake 工程名仍是 `00_basic`（教程残留），仓库名是 `ESP32_EBadge`。

---

## 芯片与模组

| 项目 | 现状 |
|------|------|
| 芯片 | **ESP32-S3**（Xtensa LX7 双核） |
| 模组 | **ESP32-S3-WROOM-1-N16R8** |
| 晶振 | 40 MHz |
| CPU 频率 | 240 MHz |
| 最低芯片版本 | Rev 0 |
| ROM | `ESP-ROM:esp32s3-20210327`（启动日志里见过） |

命名含义：

- **N16**：模组内 16 MB Flash
- **R8**：模组内 8 MB PSRAM
- **WROOM-1**：对外是 Quad SPI Flash + 内部 Octal（OPI）PSRAM  
  GPIO35 / 36 / 37 在模组内部给 PSRAM 用，板子对外没有这些脚。

`sdkconfig` 与这个型号一致：

- Flash：`CONFIG_ESPTOOLPY_FLASHSIZE_16MB`，QIO / 80 MHz，**未开** `CONFIG_ESPTOOLPY_OCT_FLASH`
- PSRAM：`CONFIG_SPIRAM=y`，`CONFIG_SPIRAM_MODE_OCT=y`，80 MHz，启动时硬件初始化并做 memtest

---

## 下载口（硬约束，改固件前必看）

本板 **只有原生 USB 能下载**，没有接到电脑的 UART0，也没有 EN 复位键。

| 脚 | 功能 |
|----|------|
| GPIO19 | USB D-（DM） |
| GPIO20 | USB D+（DP） |

电脑侧看到的是 **USB Serial/JTAG** 虚拟 COM 口。当前 VS Code 扩展配置：

- IDF：`D:/Espressif/frameworks/esp-idf-v5.5.4/`
- 口：`COM11`（会随插口变化）
- Flash 类型：UART（对这个虚拟 COM 口发 esptool，不是 CH340 那种外挂串口芯片）

必须保持：

- `CONFIG_USJ_ENABLE_USB_SERIAL_JTAG=y`
- 不要初始化 TinyUSB / USB-OTG（会抢走片内 USB PHY），**除非走配置模式 U 盘**：`usb_msc_enter()` / `usb_msc_exit()` 必须成对，退出后 PHY 还给 Serial/JTAG。不要关 `CONFIG_USJ_ENABLE_USB_SERIAL_JTAG`，不要烧相关 eFuse。
- 不要把 GPIO19 / GPIO20 当普通 GPIO
- 不要烧关掉 USB 下载 / JTAG 的 eFuse
- 不要开 Secure Boot / Flash 加密（当前都未开）

复位方式：拔插 USB。空 Flash 或非法镜像会看门狗复位，电脑侧表现为 USB 周期性掉线。详见 `USB周期性掉线排查记录.md`。

---

## 已知板级硬件（来自排查记录，仓库里暂无原理图）

电源：3.3 V 用 **SY8089A1AAC**。

| 脚 | 板上用途 | 备注 |
|----|----------|------|
| EN | 0 Ω 上拉到 3.3 V | 无 RC、无复位键 |
| GPIO0 | BOOT | 悬空，靠内部弱上拉启动 |
| GPIO1 | SD_CD | 卡检测，低有效；不要当 LED |
| GPIO2 | SD_D2 | SDMMC 4-bit |
| GPIO3 | AW9523 RSTN | 无上拉；strapping，上电不宜高阻。固件启动立刻配成推挽 |
| GPIO14 | AW9523 SCL | I2C0 |
| GPIO21 | AW9523 SDA | I2C0 |
| GPIO4 / 5 / 6 / 7 | LCD D0 / D1 / D2 / D3 | QSPI 数据 |
| GPIO15 | LCD SCL | QSPI 时钟 |
| GPIO16 | LCD CS | 片选，低有效 |
| GPIO8 | 按键：确定（kaiguan2 / KEY2） | 输入上拉，按下为低 |
| GPIO17 | LCD 背光（LED-A） | 排查记录里曾标 LED_CON；对照屏规格后按背光控制理解 |
| GPIO18 | 按键：向上（kaiguan1 / KEY1） | 输入上拉，按下为低 |
| GPIO19 / 20 | USB DM / DP | 下载 + 日志 + letter-shell |
| GPIO38 | SD_D1 | SDMMC 4-bit |
| GPIO39 | SD_D0 | SDMMC 4-bit |
| GPIO40 | SD_CLK | SDMMC 时钟 |
| GPIO41 | SD_CMD | SDMMC 命令 |
| GPIO42 | SD_D3 | SDMMC 4-bit |
| GPIO45 | 原接 ADC，已断开悬空 | strapping；上电为高会把 VDD_SPI 打成 1.8 V |
| GPIO46 | 按键：向下（kaiguan3 / KEY3） | strapping，内部下拉；固件启动后改上拉。不要板上拉到 3.3 V |

板上还有屏和 SD 卡（已确认拆掉后 USB 掉线现象仍在，说明当时掉线不是它们拉垮供电）。扩展 IO 用 **1 片 AW9523BTQR**（AD1=AD0=1，地址 `0x5B`；5 组共阳 RGB）。电流档与 20 mA 灯的对照见 `docs/AW9523虚拟IO移植记录-给接手AI.md`。**已知：等 DIM（如 RGB 50 50 50）视觉上不是白，三色不平衡，偏差待定。**

屏规格书已入库，摘要见 `docs/T180BV-C20-02屏规格摘要.md`。

| 项目 | 规格 |
|------|------|
| 型号 | **T180BV-C20-02**（金逸晨，1.8 寸圆屏） |
| 分辨率 | 360 × 360 |
| 驱动 IC | **ST77916** |
| 接口 | **QSPI**（D0~D3 + SCL + CS） |
| 颜色 | 规格书支持 16.7M / 262K / 65K；固件当前用 RGB565 |
| 背光 | 3 颗白光 LED 并联，典型 60 mA / ~3.0 V |
| 触摸 | FPC 上有 TP-SCL/SDA/INT/RST/VCC，固件未用 |
| 复位 | 规格书 RESX 低有效；固件 RST 未接 GPIO，走软复位 |

---

## 软件栈

| 项目 | 现状 |
|------|------|
| 框架 | **ESP-IDF v5.5.4** |
| 目标 | `CONFIG_IDF_TARGET="esp32s3"` |
| 蓝牙 | 未开（`CONFIG_BT_ENABLED` 未置位） |
| Wi-Fi | 组件默认开着，应用层未使用 |
| 控制台主口 | UART0，115200（板上没接到电脑） |
| 次控制台 | USB Serial/JTAG（电脑能看到的日志走这里） |
| Secure Boot | 关 |
| Flash 加密 | 关 |
| TinyUSB | **仅配置模式 MSC**（`main/usb_msc.c`）。正常模式 / 退出后 PHY 还给 USB Serial/JTAG。CDC 未开 |
| SD 卡 | 官方 `sdmmc` + `fatfs`；4-bit SDMMC，挂 `/sdcard`。U 盘前会卸 FAT，退出再挂回 |
| letter-shell | **2.0.8**，组件 `components/letter_shell/`，I/O 走 USB Serial/JTAG；源码 **UTF-8** |
| os_monitor | `components/os_monitor/`：CPU/堆/任务表；运行时统计走官方 `esp_timer`；dump 走 USB Serial/JTAG |
| AW9523 / 虚拟 IO | `components/BSP/EXIO/`，官方 `i2c_master`；`ledmode_isel=3`（约 9 mA，20 mA 灯先不改档） |
| LCD | 官方 `esp_lcd` + 托管组件 `espressif/esp_lcd_st77916` ^2.0.2；像素走一线 `0x02`/`0x2C` |
| LVGL | 官方 `espressif/esp_lvgl_port` + `lvgl/lvgl` ^9；自定义 flush，**不用** `lvgl_port_add_disp` |
| 看门狗 | 任务看门狗 5 s；中断看门狗 300 ms，双核都查 |

编译与烧录请用 **VS Code + Espressif ESP-IDF 扩展**（Build / Flash / Monitor），不要把 Cursor 当主构建环境。

---

## 分区表

自定义表：`spartitions-16MiB.csv`（16 MB Flash）

| 名称 | 类型 | 偏移 | 大小 |
|------|------|------|------|
| nvs | data / nvs | 0x9000 | 24 KB |
| phy_init | data / phy | 0xf000 | 4 KB |
| factory | app | 0x10000 | 约 1984 KB |
| vfs | data / fat | 0x200000 | 10 MB |
| storage | data / spiffs | 0xc00000 | 4 MB |

应用分区大约 2 MB；后面预留了 FAT 和 SPIFFS，方便以后放图、字库、SD 相关数据。

---

## 当前代码在干什么

目录很精简：

```
ESP32_EBadge/
├── CMakeLists.txt              # project(00_basic)
├── sdkconfig                   # ESP32-S3 / 16MB / OPI PSRAM / USB-JTAG
├── spartitions-16MiB.csv
├── main/main.c                 # 先 USB shell，再 os_monitor，再 AW9523，再 LED 脚本任务，再三键，再 lcd / lvgl
├── main/led_script.c           # SD 卡 LED.CFG：五条指令；shell ledplay/ledstop，不上电自动播
├── main/lvgl_app.c             # LVGL port + FULL DMA flush + USB 模式设置页 + sysmon
├── main/ui_usb_mode.c          # 四页 USB 模式 UI；配置模式开 MSC
├── main/usb_msc.c              # TinyUSB MSC；退出后恢复 Serial/JTAG
├── main/idf_component.yml      # st77916、esp_lvgl_port、lvgl ^9、esp_tinyusb
├── components/letter_shell/    # letter-shell 2.0.8 + USB Serial/JTAG 胶水
├── components/os_monitor/      # FreeRTOS CPU/堆/任务表；dump 走 USB Serial/JTAG
├── components/BSP/LED/         # GPIO LED 驱动（当前未用）
├── components/BSP/LCD/         # ST77916 QSPI 点亮
├── components/BSP/KEY/         # 三键：向上 GPIO18 / 确定 GPIO8 / 向下 GPIO46
├── components/BSP/SDMMC/       # SDMMC host + 卡初始化（本板脚，4-bit）
├── components/BSP/SDMMC_FAT/   # 把已 init 的卡挂到 /sdcard
├── components/BSP/EXIO/        # AW9523 + io_virtual（共阳 RGB）
├── letter_shell移植/           # 教具移植包（只读参考，不要当产品代码改）
├── os监控移植/                 # 教具 OS 监视移植包（只读参考）
├── 虚拟IO与扩展IO移植/         # 教具移植包（只读参考）
├── docs/T180BV-C20-02-*.pdf    # 屏原厂规格书
├── docs/T180BV-C20-02屏规格摘要.md
├── docs/LED样式文件-给接手AI.md
├── sdcard_kit/                 # 打包盒；把其中 LED/ 拷到 SD 根目录
├── docs/espressif/             # 乐鑫官方资料
└── .vscode/settings.json       # IDF 路径和 COM 口
```

`app_main()` **先** `shell_app_start()`（USB Serial/JTAG 上的 letter-shell），再 `os_monitor_start()`（默认不自动刷表），再 NVS，再 `io_virtual_start()`（GPIO3 立刻推挽，AW9523 任务），再 `led_script_init()`（播放任务待命，**不**自动读卡），再 `key_init()`（三键扫描），再 `sdmmc_fat_start()`（SD 失败只警告，继续启动），再 `lcd_init()` / `lvgl_app_start()`。LCD 或 LVGL 失败会空转打日志，shell、os_monitor 与按键任务仍在跑，避免看门狗复位把 USB 打掉。shell 详情见 `docs/letter-shell移植记录-给接手AI.md`。OS 监视见 `docs/OS监控移植记录-给接手AI.md`。AW9523 详情见 `docs/AW9523虚拟IO移植记录-给接手AI.md`。三键语义见 `docs/按键绑定-给接手AI.md`。LVGL 接法见 `docs/LVGL接入记录-给接手AI.md`。USB 设置页见 `docs/USB模式设置页-给接手AI.md`。SD 卡见 `docs/SD卡接入记录-给接手AI.md`。LED 样式文件见 `docs/LED样式文件-给接手AI.md`。

三键（用户 2026-08-25 更正）：**向上 = GPIO18 / KEY_1**，**确定 = GPIO8 / KEY_2**，**向下 = GPIO46 / KEY_3**。输入上拉、按下为低。驱动有消抖 / 单击 / 长按和 shell 命令 `key`。**已绑到 USB 模式设置页**（单击浏览，长按确定返回/退出）。

SD：`sdmmc_fat_start()` 挂 `/sdcard`，然后 LCD，再 LVGL 注册 `S:`。shell 命令 `sd` 列根目录。无卡只警告，不挡住屏。LED 样式在文件夹 **`LED/`**：默认 `/sdcard/LED/LED.CFG`，仓库打包在 `sdcard_kit/LED/`。`ledplay` / `ledstop` 手动播，不上电自动播。详情见 `docs/SD卡接入记录-给接手AI.md`、`docs/LED样式文件-给接手AI.md`、`sdcard_kit/README.md`。

LCD 引脚在 `components/BSP/LCD/lcd.h`：QSPI 走 GPIO4/5/6/7 + 15（时钟）+ 16（CS），背光 GPIO17 PWM，RST 未接。初始化命令在 `lcd_init_cmds.h`（**HD18004C18**；VoCat / `st77916_180` / 乐鑫组件默认花屏，不要退回）。LVGL flush 走 `lcd_draw_rgb565_1wire_area()`（一线 16-bit，**invert on**），**不要**用 `draw_bitmap`/QIO。`main/lcd_test.c` 准星图仍保留，当前不调用。点亮实验见 `docs/LCD点亮排查记录-给接手AI.md`。LVGL 见 `docs/LVGL接入记录-给接手AI.md`。

LED 驱动仍在 `components/BSP/LED/`，写死 **GPIO1**，当前 `main.c` 没有调用它。排查记录里 GPIO1 是 SD_CD，GPIO17 现按屏背光使用。

BSP 依赖官方 `driver`、`esp_lcd`、`sdmmc`、`fatfs` 和乐鑫托管 `esp_lcd_st77916`。没有第三方库、没有 Arduino。不占用 USB PHY / GPIO19 / GPIO20。

---

## 和成品板不一致、后续容易踩的点

1. **GPIO1**：留给 SD_CD，LED 驱动仍写死此脚，当前 main 未用，不要再当指示灯。
2. **GPIO17**：屏背光（规格书 LED-A）。60 mA 背光不宜 GPIO 直驱，板级开关未在仓库原理图里核对。
3. **屏复位 RESX** 固件未接 GPIO，异常起不来时优先补硬复位。
4. **触摸脚**在 FPC 上，驱动还没做。
5. **UART0 日志电脑看不到**；看输出和 **shell 输入** 都走 USB Serial/JTAG 的 COM 口。Monitor / 串口助手编码选 **UTF-8**，关掉本地回显。
6. **letter-shell 源码是 UTF-8**（移植包文档写的 GB2312 以本仓库实际文件为准）。不要再按 GBK 转码，注释会花。
7. **没有复位键**：烧录失败或空片时，需要 IO0 对地 + 拔插 USB 才能进下载。
8. **GPIO3 / 45 / 46 是 strapping**，改接法或上电电平可能影响启动和 Flash 电压。
9. 工程名仍是教程残留 `00_basic`。**SD 已按用户脚接到 SDMMC 4-bit + `/sdcard`**，见 `docs/SD卡接入记录-给接手AI.md`。配置模式 U 盘走 `main/usb_msc.c`，退出必须恢复 Serial/JTAG。**AW9523 虚拟 IO 已接入**（共阳 RGB，电流档 1/4 Imax，20 mA 灯先不改档）。**RGB 等 DIM 不是白（例 50 50 50），偏差待定**，见 `docs/AW9523虚拟IO移植记录-给接手AI.md` 第 6 节。**LED 样式在 SD 的 `LED/` 文件夹**（默认 `LED/LED.CFG`），打包见 `sdcard_kit/`，shell `ledplay`。当前 FAT 只有 8.3 短文件名。
10. **三键已绑到 USB 模式设置页。** 向上 GPIO18 / 确定 GPIO8 / 向下 GPIO46，见 `docs/按键绑定-给接手AI.md` 与 `docs/USB模式设置页-给接手AI.md`。GPIO46（向下）不要硬件上拉到 3.3 V。**配置模式会切 USB PHY 做 U 盘**；上电按住向上键强制正常模式。

---

## 文档与协作

| 文件 | 内容 |
|------|------|
| `开发者协作指南(必读).md` | 分支 + PR，不要直接推 `main` |
| `USB周期性掉线排查记录.md` | 空 Flash 导致 USB 周期性掉线的根因和烧录步骤 |
| `docs/T180BV-C20-02屏规格摘要.md` | 1.8 寸圆屏规格摘录（引脚、电源、QSPI、和固件对照） |
| `docs/LCD点亮排查记录-给接手AI.md` | LCD 点亮实验记录（已成功：实验 V） |
| `docs/letter-shell移植记录-给接手AI.md` | letter-shell 2.0.8 接到 USB Serial/JTAG；UTF-8；接手改 shell 先读 |
| `docs/OS监控移植记录-给接手AI.md` | os_monitor：esp_timer 运行时统计、IDLE0/IDLE1、内部 DRAM；shell `os_stats` |
| `docs/AW9523虚拟IO移植记录-给接手AI.md` | AW9523 虚拟 IO 接法、RGB 脚表、20 mA 灯 vs `isel=3`（先不改档）；RGB 等 DIM 非白、偏差待定 |
| `docs/按键绑定-给接手AI.md` | 三键：向上 GPIO18 / 确定 GPIO8 / 向下 GPIO46；已绑 USB 设置页 |
| `docs/LVGL接入记录-给接手AI.md` | LVGL 9 + 官方 port + QIO DMA flush；不要 `lvgl_port_add_disp` |
| `docs/USB模式设置页-给接手AI.md` | 正常/配置四页 UI；配置模式 TinyUSB MSC；退出恢复下载口 |
| `docs/SD卡接入记录-给接手AI.md` | SDMMC 4-bit 脚表、FAT `/sdcard`、禁止 MSC |
| `docs/LED样式文件-给接手AI.md` | `LED/` 下五条指令；shell `ledplay`/`ledstop`；不上电自动播 |
| `sdcard_kit/` | 往产品 SD 里拷的文件夹（先拷其中的 `LED/`） |
| `docs/T180BV-C20-02-V1-产品规格书-20260311.pdf` | 屏原厂规格书 |
| `docs/espressif/` | S3 / WROOM-1 数据手册和下载相关官方页 |
| `.cursor/rules/` | USB 下载不能丢、先问再改、优先乐鑫官方组件 |
