# 开发者协作指南

欢迎参与 **ESP32_EBadge** 项目！本仓库采用 **分支开发 + Pull Request（PR）** 的协作方式：请在独立分支上开发，完成一个阶段后提交 PR，由仓库维护者审核后合并到 `main`。

> **重要：** 请勿直接向 `main` 分支推送代码。`main` 已受分支保护，合并必须通过 PR。

---

## 开始之前

1. 确保你已被邀请为仓库协作者（Write 权限）。
2. 安装 [Git](https://git-scm.com/)，推荐使用 [VS Code](https://code.visualstudio.com/) 或 [Cursor](https://cursor.com/) 进行开发。
3. 安装 [ESP-IDF](https://docs.espressif.com/projects/esp-idf/zh_CN/latest/esp32/get-started/) 开发环境（本项目为 ESP32 工程）。
4. 克隆仓库：

```bash
git clone https://github.com/XV-lab/ESP32_EBadge.git
cd ESP32_EBadge
```

> **VS Code 快捷方式：** 按 `Ctrl+Shift+P` 打开命令面板，输入 `Git: Clone` 并回车，按提示选择目录即可克隆仓库。

---

## 日常开发流程

### 1. 同步最新的 `main`

开始新任务前，先拉取远程最新代码：

```bash
git checkout main
git pull origin main
```

| 操作 | VS Code 快捷方式 |
|------|------------------|
| 打开源代码管理 | `Ctrl+Shift+G` |
| 切换分支 | 点击窗口左下角分支名 → 选择 `main` |
| 拉取（Pull） | 源代码管理面板右上角 `...` → **拉取**，或点击状态栏同步按钮 ↻ |

---

### 2. 创建开发分支

从 `main` 新建分支，命名建议：`feature/你的名字-功能描述` 或 `fix/你的名字-问题描述`。

```bash
git checkout -b feature/你的名字-功能描述
```

示例：

```bash
git checkout -b feature/zhangsan-display-ui
git checkout -b fix/lisi-wifi-reconnect
```

| 操作 | VS Code 快捷方式 |
|------|------------------|
| 创建新分支 | 点击左下角分支名 → **创建新分支...** → 输入分支名 |
| 从远程创建分支 | 命令面板 `Ctrl+Shift+P` → `Git: Create Branch From...` |

---

### 3. 编写代码并本地验证

- 主要源码在 `main/` 目录。
- 编译产物在 `build/`，已被 `.gitignore` 忽略，**不要提交**。
- 提交前请确认项目能正常编译（`idf.py build`）。

---

### 4. 提交更改

```bash
git add .
git commit -m "简要描述本次改动"
```

提交信息建议：

- 使用中文或英文均可，表述清楚「做了什么」。
- 一行概括，必要时可在正文中补充细节。

| 操作 | VS Code 快捷方式 |
|------|------------------|
| 打开源代码管理 | `Ctrl+Shift+G` |
| 暂存单个文件 | 在更改列表中，点击文件旁的 **+** 号 |
| 暂存全部文件 | 在「更改」标题旁点击 **+** 号 |
| 取消暂存 | 点击文件旁的 **−** 号 |
| 填写提交说明并提交 | 在上方输入框写好说明后，按 `Ctrl+Enter` |
| 查看文件差异 | 点击更改列表中的文件名 |

---

### 5. 推送到远程

首次推送当前分支：

```bash
git push -u origin feature/你的名字-功能描述
```

后续在同一分支继续开发：

```bash
git push
```

| 操作 | VS Code 快捷方式 |
|------|------------------|
| 推送（Push） | 源代码管理面板 `...` → **推送**，或点击状态栏 ↑ 同步按钮 |
| 拉取并推送（Sync） | 点击状态栏 ↻ 同步按钮（先拉取再推送） |
| 发布分支（首次推送） | 推送时若远程无此分支，VS Code 会提示 **发布 Branch**，点击确认即可 |

---

### 6. 发起 Pull Request

1. 打开 GitHub 仓库页面：<https://github.com/XV-lab/ESP32_EBadge>
2. 推送后通常会看到 **Compare & pull request** 横幅，点击即可。
3. 或手动：**Pull requests** → **New pull request**
   - **base（目标分支）：** `main`
   - **compare（来源分支）：** 你的开发分支
4. 填写 PR 标题和说明，例如：
   - 做了什么
   - 如何测试
   - 是否有关联 Issue
5. 点击 **Create pull request**，等待维护者审核。

| 操作 | VS Code 快捷方式 |
|------|------------------|
| 在浏览器中创建 PR | 推送后，VS Code 右下角可能弹出提示，点击 **Create Pull Request**；或在源代码管理 `...` 菜单中查找 **Create Pull Request**（需安装 GitHub Pull Requests 扩展） |
| 查看当前 PR | 安装 [GitHub Pull Requests and Issues](https://marketplace.visualstudio.com/items?itemName=GitHub.vscode-pull-request-github) 扩展后，可在侧边栏直接管理 PR |

---

### 7. 根据审核意见修改

维护者可能会提出修改意见。在同一分支上继续开发、提交、推送即可，PR 会自动更新：

```bash
# 修改代码后
git add .
git commit -m "根据 review 意见调整 xxx"
git push
```

若 `main` 已有新提交，建议先同步再推送，减少合并冲突：

```bash
git checkout main
git pull origin main
git checkout feature/你的名字-功能描述
git merge main
# 解决冲突后
git add .
git commit -m "合并 main 最新代码"
git push
```

| 操作 | VS Code 快捷方式 |
|------|------------------|
| 合并 main 到当前分支 | 命令面板 `Ctrl+Shift+P` → `Git: Merge Branch...` → 选择 `main` |
| 解决冲突 | 冲突文件中点击 **接受当前更改 / 接受传入更改 / 保留双方**，完成后暂存并提交 |

---

## 分支命名规范

| 前缀 | 用途 | 示例 |
|------|------|------|
| `feature/` | 新功能 | `feature/wangwu-ble-provision` |
| `fix/` | Bug 修复 | `fix/chenliu-led-flicker` |
| `docs/` | 文档更新 | `docs/readme-setup` |
| `refactor/` | 重构 | `refactor/sensor-module` |

---

## 注意事项

- **不要**在 `main` 上直接开发或推送。
- **不要**提交 `build/`、`managed_components/` 等已被忽略的文件。
- 一个 PR 尽量只做一件事，方便审核。
- 若改动较大，建议先开 Issue 与维护者讨论方案。
- PR 合并后，可删除远程已合并的分支，本地执行：

```bash
git checkout main
git pull origin main
git branch -d feature/你的名字-功能描述
```

| 操作 | VS Code 快捷方式 |
|------|------------------|
| 删除本地分支 | 点击左下角分支名 → 选择 **删除分支...** |
| 清理已合并分支 | 命令面板 → `Git: Delete Branch...` |

---

## VS Code 常用快捷键速查

| 功能 | 快捷键 / 操作 |
|------|----------------|
| 命令面板 | `Ctrl+Shift+P` |
| 源代码管理面板 | `Ctrl+Shift+G` |
| 提交（光标在提交说明框内） | `Ctrl+Enter` |
| 切换 / 创建分支 | 点击左下角分支名 |
| 拉取 / 推送 / 同步 | 状态栏 ↻ ↑ 按钮，或源代码管理 `...` 菜单 |
| 打开集成终端 | `` Ctrl+` `` |
| 查看 Git 日志 | 命令面板 → `Git: View History`（部分版本需 GitLens 等扩展） |

---

## 遇到问题？

- 合并冲突：优先在 VS Code 内置合并编辑器中处理，搞不定时联系维护者。
- 权限不足：确认已被添加为协作者，且拥有 Write 权限。
- 推送被拒（`main` 受保护）：检查是否误在 `main` 上推送，应切换到自己的开发分支。

如有疑问，请在 GitHub 仓库的 **Issues** 中提出。
